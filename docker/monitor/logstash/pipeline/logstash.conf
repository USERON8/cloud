input {
  file {
    path => "/app/logs/*.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
    codec => multiline {
      pattern => "^\d{4}-\d{2}-\d{2}"
      negate => true
      what => "previous"
    }
  }
}

filter {
  # ASCII comment sanitized for compatibility.
  grok {
    match => { 
      "message" => "%{TIMESTAMP_ISO8601:timestamp} \[%{DATA:thread}\] %{LOGLEVEL:level} %{DATA:class} - %{GREEDYDATA:log_message}" 
    }
  }
  
  # ASCII comment sanitized for compatibility.
  date {
    match => [ "timestamp", "yyyy-MM-dd HH:mm:ss.SSS" ]
    target => "@timestamp"
  }
  
  # ASCII comment sanitized for compatibility.
  if [log_message] =~ /\[.*\]/ {
    grok {
      match => { 
        "log_message" => "\[%{DATA:trace_id}\] %{GREEDYDATA:message_body}" 
      }
    }
  }
  
  # ASCII comment sanitized for compatibility.
  mutate {
    remove_field => [ "message" ]
  }
  
  # ASCII comment sanitized for compatibility.
  mutate {
    rename => { "log_message" => "message" }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "cloud-logs-%{+YYYY.MM.dd}"
  }
  
  # ASCII comment sanitized for compatibility.
  stdout { 
    codec => rubydebug 
  }
}
