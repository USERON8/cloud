server:
  port: ${SERVER_PORT:80}
  tomcat:
    threads:
      max: ${SERVER_TOMCAT_THREADS_MAX:400}
      min-spare: ${SERVER_TOMCAT_THREADS_MIN_SPARE:20}
    max-connections: ${SERVER_TOMCAT_MAX_CONNECTIONS:20000}
    accept-count: ${SERVER_TOMCAT_ACCEPT_COUNT:1000}

spring:
  application:
    name: gateway
  profiles:
    active: dev
    include: route,api
  main:
    allow-bean-definition-overriding: true

  # Nacos 閰嶇疆
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
        username: nacos
        password: nacos
        namespace: public
        group: DEFAULT_GROUP
        prefer-ip-address: ${NACOS_PREFER_IP_ADDRESS:true}
        ip: ${NACOS_INSTANCE_IP:}
        port: ${NACOS_INSTANCE_PORT:${server.port}}
        ephemeral: ${NACOS_EPHEMERAL:true}
        metadata:
          instance-id: ${SERVICE_INSTANCE_ID:${spring.application.name}:${random.uuid}}
          deploy-mode: ${SERVICE_DEPLOY_MODE:multi}
          traffic-role: ${SERVICE_TRAFFIC_ROLE:business}
          singleton: ${SERVICE_SINGLETON:false}
      config:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
        username: nacos
        password: nacos
        namespace: public
        file-extension: yaml
        group: DEFAULT_GROUP

    # Gateway 閰嶇疆
    gateway:
      discovery:
        locator:
          enabled: false  # 绂佺敤鑷姩鏈嶅姟鍙戠幇,浣跨敤application-route.yml涓殑鎵嬪姩璺敱閰嶇疆
          lower-case-service-id: true
      csrf:
        enabled: true
      # 鍏ㄥ眬CORS閰嶇疆
      globals:
        cors-configurations:
          '[/**]':
            allowedOriginPatterns:
              - "http://localhost:*"
              - "http://127.0.0.1:*"
              - "https://*.yourdomain.com"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders:
              - "Authorization"
              - "Content-Type"
              - "Accept"
              - "X-Requested-With"
            allowCredentials: true
            maxAge: 3600

      # 榛樿杩囨护鍣?
      default-filters:
        - name: Security
          args:
            enableIpCheck: true
            enableRateLimit: true
            enableTokenCheck: true
            enablePerformanceMonitoring: true

  # Config 閰嶇疆
  config:
    import: optional:nacos:common

  # 闈欐€佽祫婧愰厤缃?
  web:
    resources:
      static-locations: classpath:/static/

  # OAuth2 璧勬簮鏈嶅姟鍣ㄩ厤缃?
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: ${AUTH_JWK_SET_URI:http://127.0.0.1:8081/.well-known/jwks.json}
          issuer-uri: ${AUTH_ISSUER_URI:http://127.0.0.1:8081}
    headers:
      content-security-policy: "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline'; img-src 'self' data:"
      xss-protection-enabled: true
      x-frame-options:
        mode: DENY
      cache-control: true
      content-type-options: true
      referrer-policy: "no-referrer-when-downgrade"

# 鏃ュ織閰嶇疆
logging:
  file:
    path: logs
    name: ${logging.file.path}/gateway.log
  level:
    com.cloud: INFO
    com.alibaba.nacos: WARN
    org.springframework.cloud.gateway: INFO
    org.springframework.security: WARN

# Spring Boot Actuator閰嶇疆
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,gateway
      base-path: /actuator
    enabled-by-default: true
  endpoint:
    health:
      show-details: when-authorized
      show-components: always
    info:
      enabled: true
    metrics:
      enabled: true
    prometheus:
      enabled: true
  health:
    redis:
      enabled: false  # Gateway浣跨敤Redis浣嗕笉寮轰緷璧?
    nacos:
      enabled: true
  info:
    env:
      enabled: true
    java:
      enabled: true
    os:
      enabled: true

# 搴旂敤瀹夊叏閰嶇疆
app:
  security:
    enable-test-api: false

# Knife4j 缃戝叧鑱氬悎閰嶇疆
knife4j:
  enable: true
  production: false
  basic:
    enable: false
  setting:
    language: ZH_CN
    enable-version: true
    enable-swagger-models: true
    swagger-model-name: 瀹炰綋绫诲垪琛?
    enable-footer-custom: true
    footer-custom-content: "Cloud 寰湇鍔″钩鍙?API 鏂囨。"
  gateway:
    enabled: true
    strategy: discover
    discover:
      enabled: true
      version: openapi3
      excluded-services:
        - gateway
      service-config:
        auth-service:
          group-name: 璁よ瘉鏈嶅姟
          api-docs-path: /v3/api-docs
        user-service:
          group-name: 鐢ㄦ埛鏈嶅姟
          api-docs-path: /v3/api-docs
        product-service:
          group-name: 鍟嗗搧鏈嶅姟
          api-docs-path: /v3/api-docs
        order-service:
          group-name: 璁㈠崟鏈嶅姟
          api-docs-path: /v3/api-docs
        payment-service:
          group-name: 鏀粯鏈嶅姟
          api-docs-path: /v3/api-docs
        stock-service:
          group-name: 搴撳瓨鏈嶅姟
          api-docs-path: /v3/api-docs
        search-service:
          group-name: 鎼滅储鏈嶅姟
          api-docs-path: /v3/api-docs





